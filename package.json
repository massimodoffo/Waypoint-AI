{
  "name": "waypoint-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Waypoint AI - multi-agent travel planning app",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "build": "echo 'no build step — static ES modules'"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "vitest": "^4.1.9"
  }
}
